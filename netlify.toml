[build]
  base = "."
  command = "npm install --legacy-peer-deps && node node_modules/react-scripts/bin/react-scripts.js build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
